{
	order dynamic_proxy first
}

:80 {
	encode zstd gzip

	dynamic_proxy

	@redirect expression {vars.action} == "redir"
	@reverseproxy expression {vars.action} != "redir"

	redir @redirect "{$AUTH_MANAGEMENT_API}?returnTo={uri}"
	reverse_proxy @reverseproxy {vars.upstream}
}
